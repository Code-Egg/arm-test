name: Dockermain
on:
  push:
    branches: ['main']
jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Docker build and push
        if: success()
        run: |
          echo "${{ secrets.DOCKER_NAME }}"
          echo "${{ vars.DOCKER_NAME1 }}"
          